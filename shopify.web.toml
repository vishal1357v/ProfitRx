name = "React Router"
roles = ["frontend", "backend"]
webhooks_path = "/webhooks/app/uninstalled"

[commands]
predev = "npm run prisma:generate:safe"
dev = "npx prisma migrate deploy && npm exec react-router dev"
